[phases.setup]
aptPkgs = [ "...", "docker.io", "docker-compose", "python3-distutils" ]

[phases.build]
cmds = [
  "docker-compose build --parallel"
]

[start]
cmd = "docker-compose up --build --remove-orphans"