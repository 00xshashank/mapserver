[phases.setup]
aptPkgs = [ "...", "docker.io", "docker-compose", "python3.12-distutils" ]

[phases.build]
cmds = [
  "docker-compose build --parallel"
]

[start]
cmd = "docker-compose up --build --remove-orphans"