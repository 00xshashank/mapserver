[phases.setup]
aptPkgs = [ "...", "docker.io", "docker-compose", "python3-pip", "python3-distutil" ]

[phases.build]
cmds = [
  "docker-compose build --parallel"
]

[start]
cmd = "docker-compose up --build --remove-orphans"